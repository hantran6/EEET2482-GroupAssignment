#include <iostream>
#include "Bid.hpp"
#include "Auction.hpp"

void Bid::placeBid(Auction auctionId, int amount) {
    /*
    if(Auction::getCurrentHighestBid == 0) {
        Auction::setCurrentHighestBid(amount);
    } else {
        if(Auction::getCurrentHighestBid <= amount) {
            Auction::setCurrentHighestBid(amount);
            Member::
        } else {
            std::cout << "Your placing bid has to be higher than " << Auction::getCurrentHighestBid << std::endl;
        }
    }
    */
}

void Bid::autoBidLimit(Auction auctionId, int bidLimit) {

}


int main() {
    
}